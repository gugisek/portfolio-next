# Copy to .env.local and set a strong password for /cv_edit.
# Without it the editor stays locked.
CV_EDIT_PASSWORD=
# Optional: separate secret for signing the session cookie (defaults to one derived from the password).
CV_EDIT_SECRET=
